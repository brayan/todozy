<manifest package="br.com.sailboat.todozy"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-permission android:name="android.permission.VIBRATE"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>


    <application
        android:name=".App"
        xmlns:tools="http://schemas.android.com/tools"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme"
        tools:replace="android:icon">

        <meta-data android:name="google_analytics_adid_collection_enabled" android:value="false" />
        <uses-library android:name="android.test.runner" android:required="false" />

        <receiver android:name=".ui.receivers.BootReceiver" >
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <receiver android:name=".ui.receivers.AlarmReceiver" />

        <receiver android:name=".ui.receivers.AlarmUpdateReceiver" />

        <activity
            android:name=".ui.LauncherActivity"
            android:theme="@style/AppTheme.SplashScreen">
            <intent-filter>
                <category android:name="android.intent.category.LAUNCHER" />
                <action android:name="android.intent.action.MAIN" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.task.list.TaskListActivity"
            android:launchMode="singleTop" />

        <activity android:name=".ui.task.details.TaskDetailsActivity"/>

        <activity android:name=".ui.task.history.TaskHistoryActivity"/>

        <activity android:name=".ui.task.insert.InsertTaskActivity"/>

        <activity android:name=".ui.settings.SettingsActivity"/>

        <activity android:name=".ui.about.AboutActivity"/>

    </application>

</manifest>
